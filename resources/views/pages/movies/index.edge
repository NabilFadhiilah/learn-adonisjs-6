@layout()
@slot('meta')
  <meta name="description" content="Browse our awsome movie list"/>
@end

  <h2>All Movies</h2>
  <div class="flex flex-wrap -mx-6">
    @each((movie) in movies)
    <div class="w-full lg:w-1/3 px-3">
      @!movie.card({movie, class:'w-full',showDirectorWritter:true})
    </div>
    @end
  </div>
@end
